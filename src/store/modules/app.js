const state = {
    //baseURL: 'http://localhost:5000'
    baseURL: 'https://prepster.herokuapp.com'
};

const getters = {
    baseURL: state => state.baseURL
};

const mutations = {};

const actions = {};

export default {
    state,
    getters,
    mutations,
    actions
};
